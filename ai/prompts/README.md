# 提示词管理

现有提示词仍在以下实现文件中，避免在 Markdown 中维护一份不会生效的重复副本：

- `inc/inc-ai.php`：内容任务指令、用户资料边界和图像生成提示。
- `inc/inc-pet.php`：宠物身份、真实统计约束与回答格式。

后续抽取运行时提示词时，应按用途创建独立文件，并同时接入服务端读取逻辑、缺失文件处理和必要验证。未接入加载器的文件仅作说明，不会自动生效。

模板只存公共指令；文章正文、用户问题和宠物状态作为运行时输入传入，不写入主题文件。

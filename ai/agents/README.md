# AI 角色

当前功能与实现入口（路径相对于主题根目录）：

- 内容助手：`inc/inc-ai.php`、`inc/inc-ai-admin.php`。根据作者操作生成内容或封面，沿用现有权限、预览与应用流程。
- 宠物对话：`inc/inc-pet.php`。使用真实宠物状态回答问题，不替代服务端成长、投喂和计分逻辑。

新增角色时，为每个角色创建独立说明文件，明确用途、输入、输出、可用操作和失败处理。角色说明不能代替 PHP 中的权限校验。
